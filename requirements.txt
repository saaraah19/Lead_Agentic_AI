# ─── Web Framework ──────────────────────────────────────────────────
fastapi==0.137.1           # was 0.115.6 — current stable as of Jun 2026
uvicorn[standard]==0.49.0  # was 0.34.0 — ASGI server that runs FastAPI

# ─── Data Validation ──────────────────────────────────────────────
pydantic==2.13.4           # was 2.10.4 — current stable
# pydantic-settings was listed previously but isn't imported anywhere —
# config.py reads env vars directly via os.getenv(). Removed to keep
# the dependency list honest. Re-add (pydantic-settings==2.x) only if
# you migrate config.py to a BaseSettings class.

# ─── AI / Gemini ──────────────────────────────────────────────────
google-genai==2.8.0        # was 1.2.0 — the SDK has moved a lot since
                            # then (this package ships new releases
                            # almost weekly). Check https://pypi.org/project/google-genai/
                            # for a newer patch before you deploy if
                            # it's been a while since this was pinned.

# ─── HTTP Client ──────────────────────────────────────────────────
httpx==0.28.1               # unchanged — still current as of Jun 2026

# ─── Environment Variables ────────────────────────────────────────
python-dotenv==1.0.1        # unchanged — still current, rarely needs bumping

# ─── Rate limiting ──────────────────────────────────────────────────
slowapi==0.1.9               # per-IP rate limiting for /chat and /leads/export

# ─── Testing ────────────────────────────────────────────────────────
pytest==8.3.4
pytest-asyncio==0.25.2

# ─── Optional — Phase 2 additions (uncomment if/when you need them) ──
# aiosqlite==0.20.0          # non-blocking SQLite driver (db.py currently
                             # uses blocking sqlite3 calls inside async routes)
# sentry-sdk[fastapi]==2.*   # error tracking / observability